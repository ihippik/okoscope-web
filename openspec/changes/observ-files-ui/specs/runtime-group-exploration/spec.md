## ADDED Requirements

### Requirement: Runtime groups present file summaries

Runtime group lists and details SHALL safely present the contracted file operation, process command, paths, and replacement state while preserving existing group navigation.

#### Scenario: File group is opened

- **WHEN** a user follows a file inventory group link
- **THEN** the group detail shows the file summary and existing occurrence navigation
